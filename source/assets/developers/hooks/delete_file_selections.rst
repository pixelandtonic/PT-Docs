``delete_file_selections`` Hook
==================================

This hook is called right before deleting all the relations of a file that is being deleted. 

Hook Parameters
---------------

``$file_id``
    The ID of the file being deleted.